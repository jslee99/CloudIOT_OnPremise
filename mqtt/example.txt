subscribe pi

1. listen data from pi
2. get data from fitbit server(req, res, oauth 2.0)
3. store data in S3


sudo systemctl (stop/start) mosquitto

1) mosquitto 설치하고
2) mosquitto.conf에
listener 1883 0.0.0.0
allow_anonymous true
protocol mqtt
3) mosquito -c mosquitto.conf (specifiying broker config file)
4) mosquitto실행




db.sleep_data.insert({"date":ISODate("2016-03-03T08:00:00.000"),"fitbit":"fitbit_data","sleep":"sleep_data"});